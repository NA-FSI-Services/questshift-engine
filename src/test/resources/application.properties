# Merge surefire + Quarkus test coverage into one exec file for the JaCoCo check.
# Lives here so the packaged app (failsafe) does not warn about unknown jacoco keys.
quarkus.jacoco.data-file=target/jacoco-quarkus.exec
quarkus.jacoco.reuse-data-file=true
quarkus.jacoco.report=false
